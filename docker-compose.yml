services:
  tidal-stream:
    build:
      context: .
    image: tidal-stream-research:latest
    environment:
      MPLBACKEND: Agg
      TIDAL_STREAM_PLOT: "0"
      TIDAL_STREAM_OUTPUT: ""
    volumes:
      - .:/workspace
    command: bash
